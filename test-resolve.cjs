try {
    const api = require('@better-auth/core/api');
    console.log('Successfully resolved @better-auth/core/api');
} catch (e) {
    console.error('Failed to resolve @better-auth/core/api:', e.message);
}
